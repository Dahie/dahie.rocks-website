{
  "name": "dahie.rocks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve --watch --port=8081",
    "clean": "rm -r dist/",
    "build": "npx @11ty/eleventy",
    "deploy": "npm run clean && npm run build && rsync -avz --omit-dir-times --delete dist/* unmedial:danielsenff.de-2023"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/font-awesome": "^1.0.0-beta.1",
    "@fortawesome/fontawesome-free": "^5.15.2",
    "bulma": "^0.9.2",
    "@11tyrocks/eleventy-plugin-sass-lightningcss": "^1.2.0"
  }
}